apiVersion: v1
kind: Service
metadata:
  name: daps-staker-1
spec:
  ports:
    - port: 53575
      name: regtest
    - port: 53573
      name: testnet
    - port: 53572
      name: mainnet
  selector:
    app: daps-staker-1
  sessionAffinity: None
  type: LoadBalancer
---
kind: PersistentVolume
apiVersion: v1
metadata:
  name: staker-1-pv-volume
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 0.1Gi
  accessModes:
    - ReadWriteOnce
  claimRef:
    namespace: default
    name: staker-1-pv-claim
  hostPath:
    path: "/tmp/pv/staker1"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: staker-1-pv-claim
  labels:
    app: daps-staker-1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: daps-staker-1
  labels:
    app: daps-staker-1
spec:
  selector:
    matchLabels:
      app: daps-staker-1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: daps-staker-1
      annotations:
        test: test
    spec:
      hostAliases:
      - ip: "10.10.10.4"
        hostnames:
        - "staker.1"
      containers:
      - image: r.cfcr.io/hysmagus/hysmagus/daps_min:develop
        imagePullPolicy: Always
        name: dapsd
        ports:
        - containerPort: 53575
          name: regtest
        - containerPort: 53573
          name: testnet
        - containerPort: 53572
          name: mainnet
        volumeMounts:
        - name: staker1data
          mountPath: /root/.dapscoin/
        command: ["/bin/bash","-c","dapscoind"]
      volumes:
      - name: staker1data
        persistentVolumeClaim:
          claimName: staker-1-pv-claim
      dnsPolicy: ClusterFirst
      imagePullSecrets:
      - name: regcred
      
