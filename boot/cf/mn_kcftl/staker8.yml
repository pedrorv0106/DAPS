apiVersion: v1
kind: Service
metadata:
  name: daps-staker-8
spec:
  ports:
    - port: 53575
      name: regtest
    - port: 53573
      name: testnet
    - port: 53572
      name: mainnet
  selector:
    app: daps-staker-8
  sessionAffinity: None
  type: LoadBalancer
kind: PersistentVolume
apiVersion: v1
metadata:
  name: staker-8-pv-volume
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 9Gi
  accessModes:
    - ReadWriteOnce
  claimRef:
    namespace: default
    name: staker-8-pv-claim
  gcePersistentDisk:
    fsType: ext4
    pdName: stakingnode-8-service-mn
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: staker-8-pv-claim
  labels:
    app: daps-staker-8
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 9Gi
apiVersion: apps/v1
kind: Deployment
metadata:
  name: daps-staker-8
  labels:
    app: daps-staker-8
spec:
  selector:
    matchLabels:
      app: daps-staker-8
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: daps-staker-8
    spec:
      hostAliases:
      - ip: "10.10.10.58"
        hostnames:
        - "staker.8"
      terminationGracePeriodSeconds: 3
      containers:
      - image: r.cfcr.io/hysmagus/daps_min:develop
        imagePullPolicy: Always
        name: dapsd
        lifecycle:
          preStop:
            exec:
              command: ["/bin/bash","-c","./launchdaps.sh stop"]
<<<<<<< Updated upstream
        command: ["/bin/bash","-c","./launchdaps.sh $DAEMONARGS && trap: TERM INT; sleep infinity & wait"]
        ports:
=======
              command: ["/bin/bash","-c","./launchdaps.sh $DAEMONARGS && trap: TERM INT; sleep infinity & wait"]
          ports:
>>>>>>> Stashed changes
        - containerPort: 53575
          name: regtest
        - containerPort: 53573
          name: testnet
        - containerPort: 53572
          name: mainnet
        volumeMounts:
        - name: staker8data
          mountPath: /root/.dapscoin/
      volumes:
      - name: staker8data
        persistentVolumeClaim:
          claimName: staker-8-pv-claim
      dnsPolicy: ClusterFirst
      imagePullSecrets:
      - name: codefresh-generated-r.cfcr.io-cfcr-default
      
